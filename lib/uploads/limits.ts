/** Max size for protocol / contract uploads (must match next.config serverActions.bodySizeLimit). */
export const MAX_UPLOAD_BYTES = 15 * 1024 * 1024;

/** National ID scans — kept at 10 MB for faster OCR processing. */
export const NATIONAL_ID_MAX_BYTES = 10 * 1024 * 1024;
